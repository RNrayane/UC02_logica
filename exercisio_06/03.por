programa {
  funcao inicio() {
    real total, cada
    escreva("digite o valor total do passeio: ")
    leia(total)
    cada = total / 4 
    escreva("o valor que cada um vai pagar ", cada)
    
  }
}
