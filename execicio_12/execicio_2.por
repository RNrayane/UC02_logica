programa {
    funcao inicio () {
     // declaração
      real a, b, c
     // entrada
       escreva("base:" )
       leia(a)
       escreva("altura: ")
       leia(b)
      // processamento
      c = (a * b) / 2
      //saida
      escreva("area do triangulo:",c,"\n")
    }
}